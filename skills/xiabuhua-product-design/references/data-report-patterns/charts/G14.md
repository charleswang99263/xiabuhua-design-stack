# Single Axis

类型：范式执行规范。实际实现与验收状态由项目记录；图示、状态模型和生产组件分别计数。

## 成熟度与本地证据

```json
{
  "maturity": "execution-spec",
  "implementation_claim": "完整范式规范；未声明同构生产组件已实现",
  "actual_local_evidence": "html-ppt-component-museum/assets/data-design-lab.html 提供通用示例；本条目精确实现需按合同执行并验收",
  "runtime_required": "stdlib SVG draft (unrun)",
  "fallback": "静态图与同源表格；地理/动态/查询能力缺失时明确标记，不能替代原能力"
}
```

## 使用任务

Support load, day by day

## 输入与粒度

```json
{
  "row": "one weekday/hour cell",
  "entity_id": "weekday_hour_key",
  "time": "hour 00–23 within weekday",
  "unit": "support count",
  "missing": "missing cell distinct from observed zero",
  "aggregation": "cell count",
  "cardinality": "7 weekdays × 24 hours"
}
```

## 逐记录单位

一个 weekday/hour 格的一条支持量记录

## 数据形状

weekday×hour×count singleAxis

## 视觉编码

row axis→weekday; x→hour; symbolSize=count×2.6; nonzero filter→visibility

## 尺度与单位示例

7 weekdays×24 hours；仅非零点绘制

## 布局、字体、线条与配色角色

seven independent singleAxis rows, each spanning 24 hours; only nonzero symbols are rendered; zero and missing are explained in the table

## 实际输出

Seven 24-hour singleAxis rows with nonzero count symbols and row-specific reveal

## 交互与可读替代

静态直接读；筛选结果提供逐格表

## 本地安全扩展

静态条目保留键盘可读标签和表格 fallback

## 验收

星期、小时、域值和峰值格可核验

## 执行顺序

验证输入与单位 → 按编码计算图形/槽位 → 继承主控已定视觉规则 → 接入真实输入与降级 → 对账、渲染和导出验证。没有有效数据不得补造样本。无生产源码时按本规范实现，不把规范本身称为已运行组件。

## 素材与分发边界

选用第三方代码、字体、图片、地图和媒体时核验适用许可证与分发条件，并保留必要声明。范式规范不授予第三方资产的使用权；未核验资产不可默认打包。
