# Draw-in + Counter

类型：范式执行规范。实际实现与验收状态由项目记录；图示、状态模型和生产组件分别计数。

## 成熟度与本地证据

```json
{
  "maturity": "execution-spec",
  "implementation_claim": "完整范式规范；未声明同构生产组件已实现",
  "actual_local_evidence": "html-ppt-component-museum/assets/data-design-lab.html 提供通用示例；本条目精确实现需按合同执行并验收",
  "runtime_required": "JS state + time/frame controller + reduced-motion static fallback (unrun)",
  "fallback": "静态图与同源表格；地理/动态/查询能力缺失时明确标记，不能替代原能力"
}
```

## 使用任务

H1 revenue, drawn in one stroke

## 输入与粒度

```json
{
  "row": "一个时间桶的一条增量与累计值记录",
  "entity_id": "g18_record_id",
  "time": "explicit time bucket/frame/window",
  "unit": "source card unit must be supplied with reviewed data",
  "missing": "null stays missing; zero remains observed zero",
  "aggregation": "use source-record value; no silent re-aggregation",
  "cardinality": "180 日；单序列；起点与终点明确"
}
```

## 逐记录单位

一个时间桶的一条增量与累计值记录

## 数据形状

累计增长时间序列

## 视觉编码

线位置→累计值；末值 counter→终点；reveal→非数据动画

## 尺度与单位示例

180 日；单序列；起点与终点明确

## 布局、字体、线条与配色角色

单强调色 2px 线+大数；动画不改值；宽幅累计线+末值 counter 与期间标签

## 实际输出

180 日累计线与同源末值 counter，2,600ms time-function reveal

## 时序与状态

```json
{
  "clock": "2,600ms time-function reveal",
  "frames": [
    "180-day cumulative line + counter"
  ],
  "identity": "daily date order retained; counter equals final cumulative value",
  "state": "reveal → counter tick → complete; replay guard",
  "color": "single cumulative line; no color-driven value change",
  "replay_dispose": "replay guard prevents duplicate RAF/timer; dispose cancels",
  "local_safety_extension": "skip/replay controls and reduced-motion show completed line/counter"
}
```

## 交互与可读替代

播放/跳过动画均显示最终值；静态默认完成态

## 本地安全扩展

skip/replay controls and reduced-motion show completed line/counter

## 验收

末值=累计和；动画前后数值不变

## 执行顺序

验证输入与单位 → 按编码计算图形/槽位 → 继承主控已定视觉规则 → 接入真实输入与降级 → 对账、渲染和导出验证。没有有效数据不得补造样本。无生产源码时按本规范实现，不把规范本身称为已运行组件。

## 素材与分发边界

选用第三方代码、字体、图片、地图和媒体时核验适用许可证与分发条件，并保留必要声明。范式规范不授予第三方资产的使用权；未核验资产不可默认打包。
