# Bar Race

类型：范式执行规范。实际实现与验收状态由项目记录；图示、状态模型和生产组件分别计数。

## 成熟度与本地证据

```json
{
  "maturity": "execution-spec",
  "implementation_claim": "完整范式规范；未声明同构生产组件已实现",
  "actual_local_evidence": "html-ppt-component-museum/assets/data-design-lab.html 提供通用示例；本条目精确实现需按合同执行并验收",
  "runtime_required": "JS state + time/frame controller + reduced-motion static fallback (unrun)",
  "fallback": "静态图与同源表格；地理/动态/查询能力缺失时明确标记，不能替代原能力"
}
```

## 使用任务

Eight products race

## 输入与粒度

```json
{
  "row": "one entity/year value record",
  "entity_id": "entity_id",
  "time": "year frame",
  "unit": "source value",
  "missing": "missing year is not ranked",
  "aggregation": "rank derived from value within current year",
  "cardinality": "8 entities × 8 years"
}
```

## 逐记录单位

一个产品在一个时间帧的一条名次记录

## 数据形状

实体×离散时间排名

## 视觉编码

bar length→value; y order→derived current rank; graphic→year; color→current rank

## 尺度与单位示例

8 个实体×8 个年份帧

## 布局、字体、线条与配色角色

current-year sorted horizontal bars; year is a large graphic label; current rank color changes per frame; no persistent trajectory in source view

## 实际输出

Realtime-sorted current-year bar frame with derived rank color and 1,150ms update

## 时序与状态

```json
{
  "clock": "1,150ms per automatic update",
  "frames": [
    "8 entities across 8 year frames"
  ],
  "identity": "entity_id persists across realtimeSort frames",
  "state": "year frame update → realtimeSort current bars",
  "color": "color follows current rank, not fixed entity category",
  "replay_dispose": "replay guard and timer disposal required",
  "local_safety_extension": "pause/step controls and reduced-motion show a static frame table"
}
```

## 交互与可读替代

source auto-updates one current year bar frame

## 本地安全扩展

pause/step controls and reduced-motion show a static frame table

## 验收

任一帧排序可重算，实体跨帧不跳位

## 执行顺序

验证输入与单位 → 按编码计算图形/槽位 → 继承主控已定视觉规则 → 接入真实输入与降级 → 对账、渲染和导出验证。没有有效数据不得补造样本。无生产源码时按本规范实现，不把规范本身称为已运行组件。

## 素材与分发边界

选用第三方代码、字体、图片、地图和媒体时核验适用许可证与分发条件，并保留必要声明。范式规范不授予第三方资产的使用权；未核验资产不可默认打包。
