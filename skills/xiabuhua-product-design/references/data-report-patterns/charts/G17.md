# Dynamic Stream

类型：范式执行规范。实际实现与验收状态由项目记录；图示、状态模型和生产组件分别计数。

## 成熟度与本地证据

```json
{
  "maturity": "execution-spec",
  "implementation_claim": "完整范式规范；未声明同构生产组件已实现",
  "actual_local_evidence": "html-ppt-component-museum/assets/data-design-lab.html 提供通用示例；本条目精确实现需按合同执行并验收",
  "runtime_required": "JS state + time/frame controller + reduced-motion static fallback (unrun)",
  "fallback": "静态图与同源表格；地理/动态/查询能力缺失时明确标记，不能替代原能力"
}
```

## 使用任务

Concurrent users, streaming

## 输入与粒度

```json
{
  "row": "one timestamped observation",
  "entity_id": "single_series_id",
  "time": "timestamp in rolling window",
  "unit": "concurrent users",
  "missing": "missing timestamp creates a gap; no invented point",
  "aggregation": "one observed value per refresh",
  "cardinality": "one series × 50 points"
}
```

## 逐记录单位

一个系列在一个刷新窗口的一条并发观测

## 数据形状

实时滚动并发序列

## 视觉编码

x→timestamp; y/area→concurrent_users; end label→current value; LIVE badge→live state

## 尺度与单位示例

单 series；50 点窗口；300ms 更新

## 布局、字体、线条与配色角色

single line with area fill, LIVE badge, and current end label; no multiband or multi-series encoding

## 实际输出

One 50-point live line+area window with 300ms refresh and current end label

## 时序与状态

```json
{
  "clock": "300ms refresh; rolling window of 50 points",
  "frames": [
    "one live series with line+area"
  ],
  "identity": "single series; end label marks current value",
  "state": "append observation → shift 50-point window",
  "color": "single series area color; LIVE badge is state marker",
  "replay_dispose": "interval cleared on hidden/unmount; source contract has no stale/pause state",
  "local_safety_extension": "pause/resume, keyboard focus, reduced-motion and last-window stale fallback"
}
```

## 交互与可读替代

source appends one observation and shifts the 50-point window

## 本地安全扩展

pause/resume, keyboard focus, reduced-motion and last-window stale fallback

## 验收

当前窗口、时间戳、单位、系列和断线状态可读

## 执行顺序

验证输入与单位 → 按编码计算图形/槽位 → 继承主控已定视觉规则 → 接入真实输入与降级 → 对账、渲染和导出验证。没有有效数据不得补造样本。无生产源码时按本规范实现，不把规范本身称为已运行组件。

## 素材与分发边界

选用第三方代码、字体、图片、地图和媒体时核验适用许可证与分发条件，并保留必要声明。范式规范不授予第三方资产的使用权；未核验资产不可默认打包。
