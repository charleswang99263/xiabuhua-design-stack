# Stagger Wave

类型：范式执行规范。实际实现与验收状态由项目记录；图示、状态模型和生产组件分别计数。

## 成熟度与本地证据

```json
{
  "maturity": "execution-spec",
  "implementation_claim": "完整范式规范；未声明同构生产组件已实现",
  "actual_local_evidence": "html-ppt-component-museum/assets/data-design-lab.html 提供通用示例；本条目精确实现需按合同执行并验收",
  "runtime_required": "JS state + time/frame controller + reduced-motion static fallback (unrun)",
  "fallback": "静态图与同源表格；地理/动态/查询能力缺失时明确标记，不能替代原能力"
}
```

## 使用任务

Fifty markets, one wave

## 输入与粒度

```json
{
  "row": "one market category in one single snapshot",
  "entity_id": "category_id",
  "time": "single snapshot; no time series",
  "unit": "category value",
  "missing": "missing category is excluded and reported",
  "aggregation": "source snapshot value",
  "cardinality": "50 bars"
}
```

## 逐记录单位

一个 market category 的一条计数记录

## 数据形状

50 根类别 bar

## 视觉编码

bar length→value; bar fill→value bin; x position→category order; animationDelay→entry order

## 尺度与单位示例

50 bars；36ms/index delay；640ms elasticOut

## 布局、字体、线条与配色角色

50 vertical bars on a category axis; bar height/length encodes value; palette is value-bin driven; labels follow source category order

## 实际输出

Fifty value-binned bars with 36ms indexed entrance and 640ms elasticOut settle

## 时序与状态

```json
{
  "clock": "animationDelay = index × 36ms",
  "frames": [
    "50 bars enter in category order"
  ],
  "identity": "category order is fixed by source snapshot",
  "state": "bar enter → elasticOut settle",
  "color": "value-driven palette bins",
  "replay_dispose": "one entrance timeline; cancel clears pending animation",
  "local_safety_extension": "pause/skip control and reduced-motion render all bars immediately"
}
```

## 交互与可读替代

来源是逐条 bar 入场与静态 bar 读数

## 本地安全扩展

pause/skip control and reduced-motion render all bars immediately

## 验收

全部类目原值可复核；无动画仍可读

## 执行顺序

验证输入与单位 → 按编码计算图形/槽位 → 继承主控已定视觉规则 → 接入真实输入与降级 → 对账、渲染和导出验证。没有有效数据不得补造样本。无生产源码时按本规范实现，不把规范本身称为已运行组件。

## 素材与分发边界

选用第三方代码、字体、图片、地图和媒体时核验适用许可证与分发条件，并保留必要声明。范式规范不授予第三方资产的使用权；未核验资产不可默认打包。
