# 周报速览 / Weekly 快览

类型：范式执行规范。实际实现与验收状态由项目记录；图示、状态模型和生产组件分别计数。

## 成熟度与本地证据

```json
{
  "maturity": "execution-spec",
  "implementation_claim": "完整范式规范；未声明同构生产组件已实现",
  "actual_local_evidence": "html-ppt-component-museum/assets/data-design-lab.html 提供通用示例；本条目精确实现需按合同执行并验收",
  "runtime_required": "HTML/CSS/SVG draft only; unrun",
  "fallback": "静态图与同源表格；地理/动态/查询能力缺失时明确标记，不能替代原能力"
}
```

## 使用任务

周报、运营监控、项目进展

## 逐记录单位

一页报告中的独立主张、图槽及证据模块

## 数据形状

报告主张→证据形式→限制→行动的整页叙事骨架

## 视觉编码

版心/章节顺序→阅读任务；图表槽位→审阅证据

## 尺度与单位示例

source report catalog viewport=1080; density=高; source slot topology=2 KPI cards + 2 chart cards + conclusion + dependency note

## 布局、字体、线条与配色角色

版心 1080；密度 高；色系仅作来源参考 Palm；本周 KPI + pictorial bar + chunky bars + 结论 + CDN/演示数据边界

## 实际输出

原创报告版式结构规格；不复制 source template

## 有序模块

```json
[
  {
    "id": "release_kpi",
    "required": true,
    "column": "main",
    "slot_family": "kpi",
    "content": "本周上线",
    "evidence_form": "reviewed evidence required"
  },
  {
    "id": "ticket_kpi",
    "required": true,
    "column": "main",
    "slot_family": "kpi",
    "content": "工单总量",
    "evidence_form": "reviewed evidence required"
  },
  {
    "id": "release_pictorial",
    "required": true,
    "column": "main",
    "slot_family": "pictorial-bar",
    "content": "工作日上线",
    "evidence_form": "reviewed evidence required"
  },
  {
    "id": "channel_bars",
    "required": true,
    "column": "main",
    "slot_family": "chunky-bars",
    "content": "渠道工单",
    "evidence_form": "reviewed evidence required"
  },
  {
    "id": "weekly_claim",
    "required": true,
    "column": "main",
    "slot_family": "claim",
    "content": "周结论",
    "evidence_form": "reviewed evidence required"
  },
  {
    "id": "dependency_note",
    "required": true,
    "column": "footer",
    "slot_family": "dependency",
    "content": "CDN/演示边界",
    "evidence_form": "reviewed evidence required"
  }
]
```

## 主网格与槽位

```json
{
  "columns": 2,
  "main_column": "main",
  "rail": "none",
  "width": "1080",
  "slot_count": 6,
  "required_slot_count": 6
}
```

## 来源、口径与脚注位置

```json
{
  "source_position": "footer/rail",
  "required": true,
  "content": "source, period, denominator, limitations, dependency boundary"
}
```

## 原有窄屏构造

fixed-width source sheet uses @media(max-width:640px) body padding and JS sheet.style.zoom=Math.min(1,avail/W)

## 可选移动端适配

local proposal only: preserve R12 module order, then stack required slots and collapse optional slots below evidence; do not claim source behavior

## 选择与排除

Select R12 only when its ordered module sequence and evidence forms fit; do not mix report skeletons

## 交互与可读替代

静态阅读优先；需要筛选时提供可复制表格与键盘路径

## 验收

结论、证据、限制、来源和动作均有固定槽位；窄屏/离线边界可验收

## 执行顺序

验证输入与单位 → 按编码计算图形/槽位 → 继承主控已定视觉规则 → 接入真实输入与降级 → 对账、渲染和导出验证。没有有效数据不得补造样本。无生产源码时按本规范实现，不把规范本身称为已运行组件。

## 素材与分发边界

选用第三方代码、字体、图片、地图和媒体时核验适用许可证与分发条件，并保留必要声明。范式规范不授予第三方资产的使用权；未核验资产不可默认打包。
