# 数据状态模型（实验性）

pattern-controller.js 仅是帧序号、播放状态和查询状态的纯逻辑模型。它不计算完整动态图数据、不驱动生产SVG重绘，也没有对应范式的完整浏览器验证。

当前长海报不调用这个模型；海报采用明确标为静态范式示意的精选图。不得将计数器变化、data-*属性或模型测试作为动态图已实现的证据。消费方若使用该模型，必须另行实现真实数据、图形更新、键盘/触摸交互、生命周期与验收；优先采用已有实跑组件或按完整范式规范实现。
